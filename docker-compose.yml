services:
  website:
    build: .
    container_name: rkimportandexport-site
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - n8n_default

networks:
  n8n_default:
    external: true